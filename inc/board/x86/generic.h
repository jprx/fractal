#ifndef GENERIC_X86_H
#define GENERIC_X86_H

// This is a placeholder for now, as X86 is pretty standardized.
// We keep all the constants and defs in <arch/x86.h>, as board headers
// should only track things that vary between boards in a given platform arch.

#endif // GENERIC_X86_H
